const TAG = 'Cardinal';
const ComponentsBuilder = require('./ComponentsBuilder');
const ThemesBuilder = require('./ThemesBuilder');

module.exports = {
    CARDINAL_TAG: `[${TAG}]`,
    ComponentsBuilder,
    ThemesBuilder
};